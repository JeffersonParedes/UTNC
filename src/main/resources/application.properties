spring.application.name=UTNC

# Conexión a PostgreSQL (Railway)
spring.datasource.url=jdbc:postgresql://yamabiko.proxy.rlwy.net:22151/railway
spring.datasource.username=postgres
spring.datasource.password=SqXhSDvNWBtHZGrDIEFeTmVRblZNbgEI

# JPA CORREGIDO
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Deshabilitar inicialización SQL automática
spring.sql.init.mode=never

# Deshabilitar sesiones JDBC si no usas Spring Session
spring.session.store-type=none

# Logging detallado
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# NO especifiques el dialecto - se detecta automáticamente