# JWT Configuration
# IMPORTANTE: Genera un secret seguro para producción (mínimo 256 bits / 32 caracteres)
# Puedes usar: openssl rand -base64 32
JWT_SECRET=your-very-secure-and-long-secret-key-here-minimum-256-bits

# Expiración del token JWT en segundos
# Valores comunes:
# 3600 = 1 hora
# 86400 = 24 horas (valor por defecto)
# 604800 = 7 días
JWT_EXPIRATION=86400
